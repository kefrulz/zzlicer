# Simple Flask Web App

This repository contains a minimal Flask web application.

## Requirements

- Python 3
- Flask

Install dependencies:

```bash
pip install -r requirements.txt
```

## Running

```bash
python app.py
```

The app will start on port 5000 and return "Hello, World!" at the root URL.
